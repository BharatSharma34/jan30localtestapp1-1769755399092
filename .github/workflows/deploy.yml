name: Deploy
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Sync
        run: |
          curl -X POST "https://voila-api.mmopro.in/sync-app" \
            -H "Content-Type: application/json" \
            -d '{"app":"jan30localtestapp1-1769755399092","repo":"https://github.com/BharatSharma34/jan30localtestapp1-1769755399092.git","github_token":"${{ secrets.PRIVATE_GITHUB_TOKEN }}"}'